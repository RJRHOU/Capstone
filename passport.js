const passport = require("passport");
const { Passport } = require("passport");
const GithubStrategy = require('passport-github2')

passport.useCallback(new GithubStrategy({
    clientID:
    clientSecret: ""
    callbackURL:

    Function()
""}))
 


Passport